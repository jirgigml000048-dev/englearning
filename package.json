{
  "name": "english-trainer",
  "version": "1.0.0",
  "description": "English Trainer for kids - PWA + Android",
  "private": true,
  "scripts": {
    "android:build": "node scripts/prepare-webdir.mjs && npx cap sync android && cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/cli": "^6.2.0",
    "@capacitor/core": "^6.2.0"
  }
}
